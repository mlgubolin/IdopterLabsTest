<h1>New Comment</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.comment_path(@conn, :create, @thread_id)) %>

<span><%= link "Back", to: Routes.thread_path(@conn, :show, @thread_id) %></span>
